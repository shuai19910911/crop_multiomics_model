# 研究计划

更新日期：2026-07-16

## 1. 研究目标

构建一个可审计、可复现、可证伪的作物多组学预训练研究体系，检验统一但分层的表示学习能否在多任务、少样本、跨物种/环境和缺失模态场景中带来稳定收益。

研究链条覆盖：

`基因组与变异 → 调控/表观组 → 转录组 → 蛋白/代谢组 → 组织/整株表型 → 环境与管理 → 育种决策`

物种、种质、组织、发育阶段、处理、地点、年份、试验block、平台和批次均作为条件轴，而不是事后附加的普通协变量。

## 2. 当前开放问题

以下内容必须由文献、数据和pilot证据决定，当前不预设答案：

1. 旗舰作物与首版模态组合；
2. 碱基、变异、基因、同源组、样本或个体中的基本表征单位；
3. headline下游任务及其独立统计单位；
4. 最终融合拓扑、模型宽深、参数规模与预训练目标；
5. 是否复用既有权重，以及是否具备“作物多组学基础模型”资格。

## 3. 分阶段路线

| 阶段 | 核心工作 | 主要产物 | 当前状态 |
|---|---|---|---|
| 0 | 资源、治理、风险、统计与外测协议 | 审核通过的机器合同和终结receipt | **COMPLETE** |
| 1 | 系统文献检索、双筛、代码/权重/许可核验 | 文献矩阵、可复用模型清单、证据综述 | 已授权，未执行 |
| 2 | 数据身份、许可、配对、重复折叠和外测候选审计 | 数据目录、样本manifest、许可receipt、配对矩阵 | BLOCKED |
| 3 | 三方案决策矩阵和MVP冻结 | 任务、模态、单位、基线、统计合同 | 未开始 |
| 4 | 架构/损失/资源设计与功效分析 | 冻结模型合同、power receipt | 未开始 |
| 5 | 数据冻结和泄漏门禁 | 内容寻址数据版本、外测seal、防泄漏receipt | 未开始 |
| 6 | 端到端小规模MVP | 可恢复训练、强基线和失败分析 | 未开始 |
| 7 | 预训练与缩放试点 | 数据比例×模型规模×seed证据 | 未开始 |
| 8 | 严格外部评估 | 一次性formal test、配对CI、Holm校正 | 未开始 |
| 9 | 生物学解释与论文交付 | 模型卡、数据卡、图表和可复现包 | 未开始 |

## 4. 阶段1：证据综述

- 开放检索源：Europe PMC、PubMed、arXiv；Crossref仅用于DOI元数据扩展。
- 检索主题：作物/植物序列模型、多组学、单细胞/空间、G×E、基因型到表型、少样本和跨域迁移。
- 每条候选记录版本链、训练数据、划分、强基线、外测、代码、权重及数据/代码/权重三类许可。
- 双人独立筛选；分歧必须裁决。无法访问或未核验的来源不得写成“已检索/已证实”。

## 5. 阶段2：数据审计

数据层级按以下稳定身份组织：

`物种 → 种质/群体/系谱 → site-year-trial → plot/individual → biological sample → assay/batch → file`

核心原则：

- 名称相似只产生候选边，不自动归并；
- 许可未知的数据不得训练；
- 严格跨模态对齐只允许同一生物实体或有证据的split aliquot；
- 同品种跨个体/年份、同组织异批次不能伪装成严格配对；
- 外测数据与预训练、微调、归一化、特征选择、checkpoint选择和ensemble选择完全隔离。

## 6. 候选建模路线

当前只保留待比较的设计空间，不提前宣布winner：

- 模态特异tokenizer/encoder；
- 条件感知分层融合；
- 跨模态对齐与缺失模态门控；
- masked reconstruction、cross-modal matching和contrastive alignment等候选目标；
- 分子状态、表型/G×E、基因/变异排序、跨物种/少样本迁移任务。

正式冻结前必须比较：单模态、简单拼接、early/late fusion、同架构随机初始化和任务适配强基线。

## 7. 训练与选择协议

只有阶段1—4及数据/许可门禁通过后才可启动训练：

1. **Smoke**：最小样例验证数据、梯度、恢复和日志；
2. **Baseline**：rrBLUP/GBLUP、反应规范、XGBoost/LightGBM、简单融合；
3. **Pilot**：仅validation，用于资源、稳定性和power估计；
4. **Pretraining**：至少两个模型规模、三个数据比例和多个seed；
5. **Adaptation**：1%/5%/10%/100%少样本曲线与缺失模态压力测试；
6. **Selection**：只使用训练/验证集，冻结winner公式后才能申请formal test；
7. **Formal test**：每个headline task至少5个formal seeds，配对block/hierarchical bootstrap 95% CI，多重headline比较采用Holm校正，一次性访问外测标签。

## 8. 成功、降级与停止规则

- 至少3类下游任务；至少2个独立迁移轴；至少2类缺失模态；
- 至少2个模型规模和3个数据比例；
- 与同架构随机初始化、强基线进行预算公平比较；
- 至少2类任务的配对95% CI下界高于0，才能支持稳定收益主张；
- 任一基础模型硬条件缺失时，名称降级为“预训练模型”或“多组学预测模型”；
- 三个实质不同修复方向均不能改善核心结果时停止该路线，提交负结果与失败分析。

## 9. 当前资源边界

当前仅授权阶段1治理工作；阶段2数据晋升、formal test、GPU训练和大规模下载仍未授权。任何资源放大必须在数据、许可、统计、外测和预算合同闭合后单独批准。
